# PLAN — v0.29.0 «По дате создания» (квитанция закрытого цикла)

Выпущено: **v0.29.0 (2026-09-20)**, тег `v0.29.0` на tip `main` (Release + бинарник). Тройка закрыта: отчёт ✅, визуал владельца `192.168.1.71:8081` ✅ («Визуал, ок»), явная команда владельца «Выпускай тег, версию и бинарник». В живой `PLAN.md` — строка в «Выпущено».

**Решения владельца (issue #81, обсуждение закрыто 2026-09-20):** «По дате создания» — **первый пункт «Сортировки» и новый ДЕФОЛТ** (был «По срокам»): до v0.25.0 таблица стояла «новые сверху» — естественный порядок для входящих; с появлением «По срокам»/«По активности» дефолт потерялся, нужен обратно. Невалидное `?sort=` — `400` (молчаливый fallback скрывал бы typo — аналогия `?plan=`/`?due=`). **Границы:** «По срокам»/«По активности» не переименовываются; `/api/projects/{id}/tasks` без явного sort получает новый дефолт (свежие сверху — перечень задач карточки проекта, принято).

## 29a — `?sort=created` + новый дефолт + UI: ЗАКРЫТ 2026-09-20 (PR #85, squash `8b24462`)

Бэкенд: `GET /api/tasks?sort=created` (`ORDER BY created_at DESC`, новые сверху); без `?sort=` — дефолт `created` (был `due`); валидация `""|due|updated|created` → 200, иначе **400**; `due`-порядок сохранён (просроченные выше; без срока в конце; тик-брейкер v0.27.1 «новые наверху»). Фронт: `FilterBar.vue` — «По дате создания» первый и дефолтный пункт; `stores/tasks.js` — смена вкладки сбрасывает `sort` на `created`. Тесты: Go `TestListTasks_ListSortCreatedDefault` + `TestListTasks_ListSortDueExplicit` + `TestAPI_ListTasksSort` (RED→GREEN: 200 due/updated/created; 400 bogus/CREATED/asc); unit `tasks-tabs.spec.js` (сброс на created) + `DashboardTasksTab.spec.js`; e2e `sort-created-29.spec.js` (3: дефолт = новые сверху — даже «молодая» с просроченным сроком; 400-на-мусор; UI-первый пункт) + синка `due-filters-7d.spec.js` (7a-сценарий теперь с явным `?sort=due`). Доки: `docs/api.md` § sort, ROADMAP (статус #81), CHANGELOG [0.29.0]. **Проходы:** Go 76/76 + race ok, vitest 184/184, golangci-lint 0 (unparam-устранение), eslint `--max-warnings=0` clean, **e2e 48/48, детерминизм 4/4** (serial `workers:1`), dev-стек rebuild'нут на новой статике; живой API: `?sort=bogus`→400, `due`/`created`→200. CI PR #85: Lint + Test pass. Merge: `8b24462` в `main` (ветка удалена).

## Закрытие (выпуск) 2026-09-20

- В main: `8b24462` (PR #85), CI Lint+Test pass; тройка закрыта (отчёт + визуал + явная команда владельца).
- Квитанция → `archive/PLAN.v0.29.md`; живой `PLAN.md` — строка в «Выпущено» + курсор на следующий шаг.
- **После v0.29.0 — курсор:** B-1 (наблюдение на проде 2–4 нед) → данные B-2; новые фичи — из issue (модель planning PR #82).

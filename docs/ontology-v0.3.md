# Ontology — Mailbridge

> **Статус:** черновик v0.3 (2026-09-12)
> **Цель:** зафиксировать сущности и связи процессной памяти организации.
> **Принцип:** сначала онтология — потом код. Ни строчки кода, пока модель не устойчива.
> **Стремление:** самообучающаяся процессная память организации.

## 0. Манифест

> Люди мыслят квитанциями. Квитанция — снятие показания с сигнала.
> Это не процесс, это метрика. Люди мыслят свершившимися фактами.
> Но если ты видишь процессы — ты можешь планировать и складывать факты
> так, как надо, а не наблюдать сложенный кем-то результат.
>
> Гонцу, принёсшему плохие вести, рубили голову.
> Это — мышление квитанциями. Мы строим то, что видит процесс.

Mailbridge — не таск-трекер и не почтовый клиент.
Mailbridge — **процессная память организации**: слой, который фиксирует
кто, что, когда, по какому поводу решил, что сделал, что из этого вышло,
и как это связано с другими решениями.

**Стремление:** система, которая **сама строит модель бизнеса из потока**
и **становится умнее с каждым днём**.

## 1. Система vs Реальность

**Это ключевая рамка.** Без неё всё остальное теряет смысл.

### Реальность
- **Связна.** Нет изолированных сущностей.
- **Непрерывна.** Процессы разворачиваются во времени.
- **Необъятна.** Управленец не может держать её в голове целиком.
- **Первична.** Она есть — независимо от того, видим мы её или нет.

### Система
- **Моделирует реальность, но не равна ей.**
- **Приближается** к реальности — постепенно.
- **Ограничена** вычислениями, памятью, восприятием пользователя.
- **Вторична.** Она — инструмент познания реальности.

### Что это значит
- **Система должна помогать видеть процессы**, а не требовать, чтобы их уже видели.
- **Система допускает «сирот»** — потому что пользователь ещё не увидел связь.
- **Система постепенно раскрывает структуру** — «тайное становится явным».
- **Система — тренажёр процессного мышления**, а не только инструмент.

> **Система не заменяет управленца. Она помогает быть эффективнее.**
> Когда ресурсов не хватает — качество управления компенсирует их отсутствие.
> И наоборот.

## 2. Роль системы: ПФУ 1–2

Система выполняет **первые два этапа Полной Функции Управления**:

### ПФУ-1. Выявление фактора среды
- Фиксация **входящего потока**: письма, сообщения, события.
- **Сырьё** — необработанные факты.

### ПФУ-2. Стереотип распознавания
- **Осмысление** фактов: что это, к чему относится, что значит.
- **Складывание в процессы** или **отнесение к группе**.
- **Формирование стереотипа** — повторяющиеся паттерны распознаются автоматически.

### Что это даёт управленцу
- **Не разрозненные факты**, а **систематизированный процесс**.
- **Не квитанции**, а **картина**.
- **Основа для решений**: планировать, управлять, реагировать.

### Что это значит для системы
- Система **не «управляет»** — она **готовит почву для управления**.
- Система **не «решает»** — она **показывает**.
- Система **не «заменяет»** — она **усиливает**.

## 3. Данные — не вход, а выход

**Это второй ключевой принцип.** Он меняет парадигму внедрения.

### Классическое внедрение (провал)
```
Внедряем систему → Требуем заполнить справочники →
Заказчик не может / не хочет / не понимает →
Данные плохие → Система не работает → Внедрение провалено
```

### Наш подход (прорыв)
```
Внедряем систему → Система анализирует поток →
Сама выявляет сущности → Предлагает их →
Заказчик подтверждает / поправляет →
Справочники заполняются сами → Система работает
```

### Три принципа
1. **Данные — не вход, а выход.** Не «заполните справочник». А «система выявит сама».
2. **Заказчик не знает своей реальности.** Он не замечает объектов, артефактов, процессов. Они есть, но не осознаны. Система **проявляет** их.
3. **Система — ученик, а не хозяин.** Она **не навязывает** структуру. Она **предлагает** и **учится** на подтверждениях.

### Что это даёт
- **Барьер входа = ноль.**
- **Время до первого результата = дни, а не месяцы.**
- **Качество данных растёт** со временем.
- **Заказчик видит** то, чего не замечал.
- **Система становится умнее** с каждым днём.

## 4. Выявление сущностей

### Цикл выявления
```
Поток → AI-анализ → Кандидат в сущность → Предложение →
Подтверждение / отклонение / поправка → Закрепление → Обучение
```

### Что выявляется
- **Объекты:** «Ледовая Арена», «ART IS ART», «ТРК Гранд Каньон».
- **Люди:** Бухтина, Целищева, Селенок.
- **Артефакты:** «Отчет_ответственные_Социальные сети.xlsx».
- **Решения:** «передаём соцсети».
- **Процессы:** «передача соцсетей».
- **Связи:** кто с кем, что с чем.

### Как это работает
1. **AI анализирует** поток (LLM + промпты).
2. **Извлекает кандидатов** — потенциальные сущности.
3. **Сверяет** с существующими сущностями (полнотекст + вектор через Manticore).
4. **Если новое** — создаёт `EntityCandidate`.
5. **Предлагает пользователю** подтвердить.
6. **На подтверждении** — создаёт `Entity`.
7. **На отклонении** — учится (negative example).
8. **На поправке** — уточняет модель.

### Принципы
- **Система не создаёт сущность сразу.** Только **кандидат**.
- **Пользователь подтверждает.** Не система.
- **Обучение на подтверждениях.** Со временем — точнее.
- **Прозрачность.** Пользователь видит, откуда кандидат.

## 5. Обучение системы

### Что обучается
- **Точность выявления** сущностей.
- **Точность связей** между сущностями.
- **Точность привязки** фактов к процессам.
- **Точность определения** типов (kind, state).

### Как обучается
- **Подтверждение** → положительный пример.
- **Отклонение** → отрицательный пример.
- **Поправка** → уточнение.
- **Повторение** → закрепление стереотипа.

### Что это даёт
- **Система становится умнее** без ручного вмешательства.
- **Качество данных растёт** со временем.
- **Нагрузка на пользователя падает**.
- **Справочники пополняются сами**.

### Принципы
- **Система не «знает» — она «учится».**
- **Пользователь не «заполняет» — он «подтверждает».**
- **Обучение — не разовое, а непрерывное.**

## 6. Сущности (узлы графа)

### 6.1. Процесс (Process)
Непрерывный сигнал, разворачивающийся во времени.
**Долгоживущая сущность.** Может содержать другие процессы.

| Атрибут | Тип | Описание |
|---|---|---|
| id | PK | |
| title | string | |
| description | text | |
| root_decision_id | FK → Decision? | |
| parent_process_id | FK → Process? | Вложенность |
| owner_id | FK → Person? | Может отсутствовать |
| priority | enum? | Может отсутствовать |
| state | enum | `active` / `paused` / `completed` / `cancelled` / `stalled` |
| started_at | timestamp | |
| closed_at | timestamp? | |
| created_at, updated_at | timestamp | |

**Принципы:**
- Owner может отсутствовать — диагностика.
- Priority может отсутствовать — норма.
- Процесс может содержать процессы.
- `stalled` — результат бездействия, а не свойство.

**TODO:**
- Нужен ли `process_kind`?
- Как отличить `paused` от `stalled`?
- Нужен ли `health`?

### 6.2. Факт / Квитанция (Fact)
Точка отсчёта в процессе. **Первичен.** Может существовать без процесса.

| Атрибут | Тип | Описание |
|---|---|---|
| id | PK | |
| process_id | FK → Process? | Может отсутствовать |
| source | enum | `email` / `telegram` / `web_form` / `manual` / `meeting` |
| source_id | string | |
| author_contact | string | |
| author_name | string | |
| subject | string | |
| body | text | |
| received_at | timestamp | |
| state | enum | `new` / `processed` / `archived` |
| meta | JSON | |
| linked_at | timestamp? | |

**Принципы:**
- Может быть без процесса — временное состояние.
- AI помогает найти процесс.

**TODO:**
- `Fact` и `inbox_item` — одна сущность или разные?
- Нужен ли `confidence`?

### 6.3. Задача (Task)
Действие, порождённое фактом. **Следствие.** Может быть без процесса.

| Атрибут | Тип | Описание |
|---|---|---|
| id | PK | |
| process_id | FK → Process? | Может отсутствовать |
| fact_id | FK → Fact? | |
| title | string | |
| description | text | |
| status | enum | `new` / `in_progress` / `completed` / `closed` / `cancelled` |
| priority | enum | |
| assignee | string | |
| due_at | timestamp? | |
| created_at, updated_at | timestamp | |

**Принципы:**
- Задача без процесса — норма.
- Система помогает найти процесс.

**TODO:**
- Как система предлагает привязать задачу?
- Нужен ли `detected_process_id`?

### 6.4. Артефакт (Artifact)
Созданный объект: документ, файл, доступ, ссылка, запись. Может быть без процесса.

| Атрибут | Тип | Описание |
|---|---|---|
| id | PK | |
| process_id | FK → Process? | |
| task_id | FK → Task? | |
| kind | enum | `document` / `file` / `access` / `link` / `record` |
| title | string | |
| hash | string? | |
| storage_path | string? | |
| url | string? | |
| content | text? | |
| version | int | |
| created_at, updated_at | timestamp | |

**Принципы:**
- В реальности — всегда в процессе. В системе — может быть сиротой.
- Система ищет адрес артефакта.

**TODO:**
- `artifact_related_to_artifact` — как?
- Может ли быть общим?
- Нужна ли история версий?

### 6.5. Человек (Person)
Участник процесса. Действующее лицо.

| Атрибут | Тип | Описание |
|---|---|---|
| id | PK | |
| name | string | |
| email | string? | |
| telegram | string? | |
| role | string? | |
| org | string? | |
| is_internal | bool | |
| created_at, updated_at | timestamp | |

**Принципы:**
- Person ≠ User.
- В реальности — одно лицо. В системе — разные роли.

**TODO:**
- Как связать Person и User?
- Нужна ли история ролей?

### 6.6. Объект (Object)
То, к чему относится процесс.

| Атрибут | Тип | Описание |
|---|---|---|
| id | PK | |
| name | string | |
| kind | enum | `site` / `place` / `system` / `department` / `account` / `other` |
| parent_id | FK → Object? | |
| meta | JSON | |
| created_at, updated_at | timestamp | |

**Принципы:**
- Глубина иерархии — по уровню рассмотрения.
- Система не навязывает.

**TODO:**
- Строгий enum или свободный?
- Как быть с пересекающимися объектами?

### 6.7. Решение (Decision)
Корень процесса. Может породить множество процессов.

| Атрибут | Тип | Описание |
|---|---|---|
| id | PK | |
| title | string | |
| description | text | |
| author_id | FK → Person | |
| decided_at | timestamp | |
| context | text | |
| source_fact_id | FK → Fact? | |
| created_at | timestamp | |

**Принципы:**
- Одно решение → много процессов.
- Система схлопывает до одного с вложенностью.

**TODO:**
- Может ли решение быть неявным?
- Как фиксировать неформальные решения?

### 6.8. Сущность (Entity) — новое
**Обобщённая сущность**, выявленная системой. Может быть объектом, человеком, артефактом, решением, процессом — или **чем-то новым**.

| Атрибут | Тип | Описание |
|---|---|---|
| id | PK | |
| kind | enum | `object` / `person` / `artifact` / `decision` / `process` / `unknown` |
| title | string | |
| description | text? | |
| canonical_id | FK → ? | Ссылка на конкретную сущность, если типизирована |
| extracted_from_fact_id | FK → Fact? | Откуда выявлена |
| confidence | float | Уверенность AI |
| state | enum | `candidate` / `confirmed` / `rejected` / `merged` |
| created_at, updated_at | timestamp | |

**Принципы:**
- Entity — **обобщение**. Конкретные сущности (Object, Person...) — **частные случаи**.
- Entity может быть **не типизирована** — «unknown». Это норма.
- Entity **не создаётся сразу** — только `EntityCandidate`.

**TODO:**
- Нужна ли Entity вообще, или достаточно конкретных сущностей?
- Как связать Entity с конкретной сущностью (Object/Person/...)?
- Что делать с `unknown`?

### 6.9. Кандидат в сущность (EntityCandidate) — новое
Предложенная сущность, ждёт подтверждения.

| Атрибут | Тип | Описание |
|---|---|---|
| id | PK | |
| proposed_kind | enum | `object` / `person` / ... |
| title | string | |
| context | text | Контекст, в котором выявлена |
| source_fact_id | FK → Fact | |
| confidence | float | |
| state | enum | `pending` / `confirmed` / `rejected` / `merged` |
| confirmed_by | FK → Person? | |
| confirmed_at | timestamp? | |
| created_at | timestamp | |

**Принципы:**
- Кандидат — **не сущность**. Он ждёт решения.
- Подтверждение → создаётся Entity.
- Отклонение → negative example для обучения.
- Слияние → кандидат = существующая сущность.

**TODO:**
- Как долго живёт кандидат?
- Что если кандидатов много?
- Как показывать пользователю?

### 6.10. Связь сущностей (EntityRelation) — новое
Ребро графа между сущностями.

| Атрибут | Тип | Описание |
|---|---|---|
| id | PK | |
| from_entity_id | FK → Entity | |
| to_entity_id | FK → Entity | |
| relation_type | string | `authored` / `assigned` / `created` / `relates_to` / ... |
| confidence | float | |
| source_fact_id | FK → Fact? | Откуда выявлена |
| state | enum | `candidate` / `confirmed` / `rejected` |
| created_at, updated_at | timestamp | |

**Принципы:**
- Связь тоже может быть кандидатом.
- Связь может быть подтверждена или отклонена.
- Связь — **не жёсткая**. Она может быть уточнена.

**TODO:**
- Нужны ли типы связей как строгий enum?
- Как разрешать конфликты связей?
- Нужна ли история изменений связей?

### 6.11. Единица памяти (MemoryUnit) — новое
Единица памяти агента (по аналогии с Hindsight).
Что-то, что агент запомнил и использует для принятия решений.

| Атрибут | Тип | Описание |
|---|---|---|
| id | PK | |
| entity_id | FK → Entity? | |
| fact_id | FK → Fact? | |
| content | text | |
| kind | enum | `observation` / `inference` / `decision` / `lesson` |
| embedding | vector | Для поиска |
| created_at | timestamp | |

**Принципы:**
- Память — **не просто факты**. Это **осмысленные единицы**.
- Память может быть **наблюдением**, **выводом**, **решением**, **уроком**.
- Память используется агентом для **будущих решений**.

**TODO:**
- Нужна ли MemoryUnit отдельно от Fact?
- Как MemoryUnit связана с Entity?
- Как использовать MemoryUnit в RAG?

## 7. Связи (рёбра графа)

| Связь | От | К | Описание |
|---|---|---|---|
| `process_has_fact` | Process | Fact | |
| `fact_spawns_task` | Fact | Task | |
| `task_creates_artifact` | Task | Artifact | |
| `process_creates_artifact` | Process | Artifact | |
| `process_involves_person` | Process | Person | |
| `task_assigned_to` | Task | Person | |
| `fact_authored_by` | Fact | Person | |
| `process_relates_to_object` | Process | Object | |
| `object_responsible_person` | Object | Person | |
| `fact_refines_fact` | Fact | Fact | |
| `process_related_to_process` | Process | Process | |
| `decision_roots_process` | Decision | Process | |
| `process_contains_process` | Process | Process | Вложенность |
| `entity_extracted_from_fact` | Entity | Fact | **новое** |
| `entity_confirmed_by_person` | Entity | Person | **новое** |
| `entity_related_to_entity` | Entity | Entity | **новое** |
| `candidate_promoted_to_entity` | EntityCandidate | Entity | **новое** |
| `memory_unit_about_entity` | MemoryUnit | Entity | **новое** |

**TODO:**
- `artifact_related_to_artifact` — как?
- Временные связи — как?
- Нужен ли `confidence` на рёбрах?

## 8. Инварианты

1. **Факт первичен.** Задача — следствие.
2. **Процесс — долгоживущая сущность.** Задача — точка. Процесс — линия.
3. **Система ≠ Реальность.** Система приближается к реальности.
4. **Сироты — норма.** Временное состояние.
5. **Процесс может быть без owner.** Диагностика, не баг.
6. **Одно решение → много процессов.** Вложенность.
7. **Глубина иерархии — по уровню рассмотрения.**
8. **Всё имеет время.** Хронология — первоклассная сущность.
9. **Система — тренажёр процессного мышления.**
10. **Система не заменяет управленца. Она усиливает.**
11. **Данные — не вход, а выход.** Справочники растут сами.
12. **Система — ученик, а не хозяин.** Предлагает, не навязывает.
13. **Сущность — не создаётся сразу.** Только кандидат.
14. **Обучение — непрерывное.** Не разовое.
15. **Прозрачность.** Пользователь видит, откуда кандидат.

## 9. Сироты и подсветка

### Что такое «сирота»
Сущность, которая **в реальности** принадлежит процессу, но **в системе** связь не установлена.

- Задача без процесса.
- Факт без процесса.
- Артефакт без процесса.
- Процесс без owner.
- Процесс без движения (`stalled`).
- Кандидат в сущность без подтверждения.

### Что делает система
- **Подсвечивает** сирот.
- **Предлагает** связь (через AI, кластеризацию, граф).
- **Помогает** пользователю увидеть процесс.
- **Не навязывает** — предлагает.

### Зачем
- **«Тайное становится явным».**
- **Управленец видит** скрытые связи.
- **Качество управления растёт** без роста ресурсов.

## 10. Справочники как результат

### Классический подход
Справочники — **вход**. Заполняются перед внедрением.

### Наш подход
Справочники — **результат**. Растут из потока.

### Что это значит
- **Не «заполните справочник объектов»**, а «система выявит объекты».
- **Не «внесите всех сотрудников»**, а «система найдёт их в переписке».
- **Не «опишите процессы»**, а «система проявит их».
- **Справочники — живые.** Они меняются вместе с бизнесом.

### Принципы
- **Барьер входа = ноль.**
- **Качество данных растёт** со временем.
- **Пользователь — не оператор, а эксперт.** Он подтверждает, а не вводит.

## 11. Тест онтологии: кейс «Передача соцсетей»

### 11.1. Решение
«Передаём ведение соцсетей части объектов»
Автор: TODO
Дата: TODO
Контекст: TODO

### 11.2. Процесс
**«Передача ведения соцсетей части объектов»**
Состояние: `active`

### 11.3. Факты
| # | Автор | Суть |
|---|---|---|
| F1 | Бухтина А.А. | Просит проверить/дополнить файл |
| F2 | Целищева В.А. | Спрашивает про Ледовую Арену |
| F3 | Целищева В.А. | Спрашивает про Лидер Спорт |
| F4 | Целищева В.А. | Дополняет таблицу ссылками |
| F5 | Селенок А.Ю. | Требует подключить отдел маркетинга |
| F6 | Билинская В.В. | Просит доступ к админ-панели |
| F7 | рядов К. | Выдал доступ |

### 11.4. Задачи
| # | Из факта | Задача | Исполнитель | Статус |
|---|---|---|---|---|
| T1 | F1 | Проверить/дополнить файл | ? | ? |
| T2 | F2 | Найти ответственного | ? | ? |
| T3 | F3 | Найти ответственного | ? | ? |
| T4 | F4 | — | — | done |
| T5 | F5 | Подключить отдел маркетинга | ? | ? |
| T6 | F6 | Выдать доступ | рядов К. | done |
| T7 | F7 | — | — | done |

### 11.5. Артефакты
| # | Название | Kind |
|---|---|---|
| A1 | «Отчет_ответственные_Социальные сети.xlsx» | document |
| A2 | «Социальные сети.xlsx» | document |
| A3 | Досье на сайты | document |
| A4 | Доступ Билинской | access |

### 11.6. Выявленные сущности (пример)
| Кандидат | Тип | Откуда |
|---|---|---|
| Ледовая Арена | object | F2, F5, F6 |
| ART IS ART | object | F1 |
| Бухтина А.А. | person | F1 |
| Целищева В.А. | person | F2, F3, F4 |
| «Социальные сети.xlsx» | artifact | F4 |
| «Передача соцсетей» | process | F1–F7 |

### 11.7. Связи (пример)
```
D1 (решение: передать соцсети)
 └─ P1 (процесс: передача соцсетей)
     ├─ F1 → T1 → A1
     ├─ F2 → T2
     ├─ F3 → T3
     ├─ F4 → A2
     ├─ F5 → T5 → A3
     ├─ F6 → T6 → A4
     └─ F7 (закрытие)

Entity: Ледовая Арена
 ├─ extracted_from: F2, F5, F6
 ├─ related_to: ART IS ART (оба — объекты)
 └─ responsible: Байбакова, Билинская
```

## 12. Открытые вопросы (TODO для автора)

### 12.1. Сущности
1. `Fact` и `inbox_item` — одна сущность или разные?
2. `Person` и `User` — как связать?
3. Может ли артефакт быть общим?
4. Нужна ли история версий артефакта?
5. Может ли решение быть неявным?
6. **Нужна ли Entity вообще, или достаточно конкретных сущностей?**
7. **Как связать Entity с конкретной сущностью?**
8. **Что делать с `unknown`?**
9. **Нужна ли MemoryUnit отдельно от Fact?**

### 12.2. Связи
10. `artifact_related_to_artifact` — как?
11. Временные связи — как?
12. Нужен ли `confidence` на рёбрах?
13. **Нужны ли типы связей как строгий enum?**
14. **Как разрешать конфликты связей?**

### 12.3. Состояния
15. Как отличить `paused` от `stalled`?
16. Нужен ли `health`?
17. Нужен ли `process_kind`?

### 12.4. Иерархия
18. Как быть с пересекающимися объектами?
19. Строгий enum для объектов?

### 12.5. Поведение системы
20. Как система предлагает привязать задачу?
21. Что если задача не находит процесс?
22. Нужен ли `detected_process_id`?
23. **Как долго живёт кандидат?**
24. **Что если кандидатов много?**
25. **Как показывать пользователю кандидатов?**

### 12.6. Обучение
26. **Как система учится на отклонениях?**
27. **Как избежать деградации?**
28. **Нужен ли «человеческий» аудит обучения?**
29. **Как измерять качество выявления?**

### 12.7. Философия
30. Как система тренирует процессное мышление?
31. Как избежать навязывания структуры?
32. Что делать с процессами, которые никто не хочет называть?

## 13. Следующие шаги

- [ ] Изучить **Hindsight** (структура БД, логика, промпты).
- [ ] Разложить **второй кейс**.
- [ ] Ответить на **открытые вопросы**.
- [ ] Спроектировать **модель данных** (граф в Postgres).
- [ ] Спроектировать **слой выявления** сущностей.
- [ ] Интегрировать **Manticore**.
- [ ] Показать **команде**.
- [ ] Только потом — код.

## 14. Философская рамка

> **Система ≠ Реальность.** Реальность — связна. Система — приближается.
> Задача системы — помогать видеть процессы, а не требовать, чтобы их уже видели.
>
> **Стандартный пользователь не умеет видеть процессы.** Это умение надо тренировать.
> Поэтому задача может быть создана без процесса. И это — норма.
>
> **Бизнес — не структура.** Процессы складываются вопреки, а не по приказу.
> Персоны с опытом формируют связи. Формальные приказы — это уже устоявшиеся связи.
>
> **Система — это ПФУ-машина.** Фиксирует факты (ПФУ-1), распознаёт стереотипы (ПФУ-2),
> складывает в процессы. Управленец получает не разрозненные факты, а систематизированный процесс.
>
> **Процесс без owner — не баг, а диагностика.** Система подсвечивает зону риска.
>
> **«Завис» — не состояние процесса, а результат чьего-то бездействия.**
>
> **Глубина иерархии — по уровню рассмотрения управленца.** Система не навязывает.
>
> **Одно решение → много процессов.** Они вложены. Система схлопывает до одного.
>
> **Система не заменяет управленца. Она усиливает.**
> Когда ресурсов не хватает — качество управления компенсирует их отсутствие.
> И наоборот.
>
> **Данные — не вход, а выход системы.** Классическое внедрение требует заполнить справочники.
> Это провал. Наш подход — система сама выявляет сущности из потока.
>
> **Заказчик не знает своей реальности.** Он не замечает объектов, артефактов, процессов.
> Они есть, но не осознаны. Система проявляет их.
>
> **Система — ученик, а не хозяин.** Она не навязывает структуру.
> Она предлагает и учится на подтверждениях.
>
> **Справочники — результат, а не вход.** Они растут со временем.
>
> **Барьер входа = ноль.** Это снимает главный страх внедрения.
>
> **Система становится умнее с каждым днём.** Это не «инструмент», а «живой организм».
>
> **Стремление:** самообучающаяся процессная память организации.

# ROADMAP — Mailbridge (декларация запланированных фич)

> **Статус:** чистый файл (2026-09-17). Философский/концептуальный уровень живёт в `docs/` контейнера
> (онтология и т.п.) — там НЕ должно дублироваться то, что здесь.
>
> **Что это:** декларация **запланированных фич** — «что и зачем», без разбивки на шаги.
> Разбивка на шаги происходит только в `PLAN.md`, в момент старта (режим B → решения → старт режима A).
>
> **Обязательство (2026-09-17):** этот файл **пополняется явно** — источник: `BACKLOG.md` (захваченные идеи),
> режим B (обсуждение), онтология (`docs/` контейнера — когда решение «вытащить во фичу» принято владельцем).
> Пустые строки НЕ живут: идея либо оформлена как фича с открытыми вопросами, либо не записана.

## Формат записи

```
### <Короткое имя фичи>
**Зачем/боль.** 1–2 предложения: чью боль закрываем.
**Границы фичи.** что в ней / чего явно нет (не смешиваем с другими фичами).
**Открытые вопросы.** (для режима B — пока не решены; по 1–4шт.)
**Статус:** заявлено | в обсуждении | в плане (шаги в PLAN.md) | выпускено (vX.Y.Z, архив)
```

## Фичи

### Отложено (явно зафиксировано владельцем — это решения, не идеи)
- **SLA-нормативы** — статус: отложено (шаг 7, v0.25.0). Детали: `archive/PLAN.v0.25.md`.
- **Напоминания (push/WS)** — статус: отложено (шаг 7, v0.25.0). Детали: `archive/PLAN.v0.25.md`.
- **`due_origin`** (происхождение срока) — статус: отложено (шаг 7, v0.25.0). Детали: `archive/PLAN.v0.25.md`.
- **Авто-принятие AI-сроков** (флаг сейчас: false — принятие вручную намеренное) — статус: отложено (шаг 7, v0.25.0). Детали: `archive/PLAN.v0.25.md`.
- **B-1 — Наблюдение на проде** (2–4 нед. метрик: `confirmed=false`, дубли, частота ручных подтверждений c 6-G; SQL-срезы по `persons`/`person_identities`) — фоновое, не блокирует очередь. Детали: `archive/PLAN.v0.24.md`.
- **B-2 — Правило авто-подтверждения** — ОТКРЫТО до данных B-1 (кандидаты: «ручное закрывает вопрос» / «N писем + стабильное имя» / «merge подтверждает персону»). Решение — только по данным B-1.

### Что НЕ делаем (границы, чтобы не предлагать повторно)
- «Починить storage/attachments» отдельным планом — НЕ делаем (решение владельца; legacy-баги — только отдельными шагами/багами).
- Переписывать legacy AI-саммари (`author='user'`, `ai_processed=-1`) — НЕ делаем; новые вердикты — только по шагу 5.
- Дубли письма (dedup) в цепочке — НЕ делаем (решение владельца: дубли ок).
- Plane как бэкенд задач — НЕ делаем (удалён в v0.22, ADR-0001).
- v1.0.0 сейчас — НЕ делаем (v0.x-циклы).

### Версия в UI
**Зачем/боль.** При визуальном чеке v0.26.0 (инцидент: устаревшая статика на 8081) было невозможно в браузере увидеть, какая сборка реально подана; версия читалась только через `mailbridge version` бинарника.
**Границы фичи.** `GET /api/version` (публичный, JSON `{version, commit, built}`) + отображение в шапке Dashboard: версия в строке, commit + time сборки в tooltip; `make run-dev` вшивает те же значения (ldflags) — commit виден и в dev. НЕ «проверка наличия новой версии»/уведомления — отдельная тема (если понадобится — новая идея).
**Решения (владелец, 2026-09-18):** 1) эндпоинт **публичный** (без JWT); 2) версия в строке, commit+built — в tooltip; 3) значения показывать **как есть** (в т.ч. `dev`/`none`) — это полезнее: в dev видно реальную сборку; 4) порядок — эта фича = следующая (v0.27.0), «Сегодня/спринты» — после неё. Версия не жёстко привязана к фиче — номер = свойство артефакта «шаг завершён».
**Статус:** **ВЫПУЩЕН (v0.27.0, 2026-09-18; тег → Release, бинарник `0.27.0 (commit: 800a62f)`)** — PR #60 → `main` (CI green). `GET /api/version` публичный + бейдж версии в шапке Dashboard (commit+built в tooltip); `make run-dev` вшивает ldflags. Тесты: `internal/web/version_test.go`, `frontend/tests/views/DashboardVersionBadge.spec.js`, e2e `version-header.spec.js`. Квитанция: `archive/PLAN.v0.27.md`.

### «Сегодня» / «План» (v0.28.0)
**Зачем/боль.** При 50+ задачах — ответ «что я делаю сегодня/завтра/на этой неделе»: фокусировка на пике, не копание по списку.
**Концептуальное ядро (решение владельца, 2026-09-19):** у задачи **две независимые даты** — `due_date` (обещание, срок исполнения; просрочка = нарушенное обещание) и **`scheduled_date` (план: когда я её делаю)**; сейчас из-за одной даты «кидаю на сегодня» = переписывание обещания ради организации дня.
**Решения (владелец, 2026-09-19, режим B):**
1. «Сегодня» = `scheduled_date = сегодня` **+ просроченные по due** (горящее — тоже видно в плане). «Намеренно совсем не видеть просроченные» — не делаем (крайний сценарий, отклонён).
2. Вкладки (решение финализировано 2026-09-20 — **вкладки-дропдауны**, аналогия Bootstrap «Tabs with dropdowns»; таб = пункт выбора, отдельный селект — НЕ нужен): статусные (Все/Активные/Бэклог/Выполненные/Закрытые) — меню вкладки «Статус»; план (Все/Без плана/Сегодня/Завтра/Неделя) — меню вкладки «План»; состояние в URL (`?tab=...`, `?status=`/`?plan=`), старое `?tab=active` и т.п. — **ломается** (закладок нет, один пользователь).
3. **Порядок вкладок: Инбокс, Статус, План, Проекты, Персоны** (по просьбе владельца — рабочие вкладки спереди).
**Границы фичи.** Поле `scheduled_date` + API-срез «План» (сегодня/завтра/неделя/без плана/все + просроченные due) + вкладки-дропдауны «Статус»/«План» (таб = пункт выбора) + поле scheduled в UI (карточка/список). НЕ полноценные спринты (кандидат 3, риск «планировщик-дубль»), НЕ SLA/напоминания, НЕ «моё»/assignee, НЕ AI-извлечение scheduled (только руками). Планирование ≠ статус: статус задачи при «План» не меняется.
**Статус:** решения зафиксированы (в PLAN — детали шага v0.28.0); старт = новая сессия, режим A.

### Здоровье e2e: 2 падающих теста (bulk-actions, projects-to-tasks C)
**Зачем/боль.** В dev-окружении (vite :5173 + backend :8081) в полном e2e-прогоне стабильно падают `bulk-actions.spec.js` (2 шт.) и `projects-to-tasks.spec.js` (C «фильтр «Модуль»»); в CI — зелёные. Baseline (28c, 2026-09-19): на чистом main те же 2 → не регрессия от 28c.
**Границы фичи.** ТОЛЬКО расследование причин (тесты / окружение-гонка / баг кода) + GREEN полный прогон в dev-окружении. Детали, факты, шаги — **issue #75**.
**Статус:** **ОБЪЯВЛЕНО** (решение владельца, 2026-09-19): старт — после merge 28d + выпуск v0.28.0. Трейкер: **issue #75**.

# Mailbridge — план (текущий)

> **Методология** — `CONTRIBUTING.md`, раздел «Методология разработки» (режимы, уровни, чек-листы, стоп-гейт). Этот файл — только курс: где стоим и что дальше.
> **Схема v0.23**: живой план = только открытые шаги. Выпущенные/завершённые блоки уходят в `archive/`, в этом файле — **одна строка** в «Выпущено».
> **Рестарт сессии (промпт)**: «план `site/mailbridge/PLAN.md`, skill `mailbridge-dev`; с какого шага читать — „Статус“ в шапке; чек-лист СТАРТА».

## Статус (курсор — читать первым)

**Дата обновления:** 2026-09-19 (**v0.27.3 — ВЫПУЩЕН** (тег `v0.27.3` → `218d6e9`, GitHub Release published, бинарник `mailbridge 0.27.3 (commit: 218d6e9)` ✅ проверен): PR #70 quote-цитата в AI-саммари + PR #71 тест-флейк TaskTableDue UTC/локаль; 153/153, CI pass; тройка: визуал владельца «ок» + команда «выпускай тег», 2026-09-19). **Осталась команда владельца: деплой :8080 `deploy-released.sh v0.27.3`**; дальше — «Сегодня/спринты» (v0.28.0+). **B-1** — фоновое наблюдение.

**Версия.** **v0.25.0** — ВЫПУЩЕН 2026-09-17 (тег `v0.25.0` → Release; = шаги 7a + 7b + 7c + фикс календаря PR #46 + хотфикс 6-H PR #48). **v0.26.0** — выпущен 2026-09-17 (шаг 7d: вкладка «Все» + сортировки + фильтры по срокам). **Модель версий (FIX 2026-09-17): шаг = версия** — каждый закрытый шаг получает свой тег `0.x.y`; hotfix-исправления = patch `0.x.y+1` от последнего тега (CONTRIBUTING §«Хотфикс»). Живой файл = только курс + шаги ≥ курсора.
**Текущий шаг:** **v0.27.3 ВЫПУЩЕН 2026-09-19 (тег `v0.27.3` → `218d6e9` + Release + бинарник ✅; визуал владельца + команда владельца).** **Далее: деплой :8080 по явной команде владельца (`deploy-released.sh v0.27.3`)**; потом — «Сегодня/спринты» (v0.28.0+, ROADMAP — режим B). **B-1** (наблюдение на проде по метрикам confirmed) идёт **в фоне и НЕ блокирует ни один кодовый шаг**; **B-2** (правило авто-подтверждения) — отдельная мини-фича, только по данным B-1, не в очереди.

## Что уже работает (карта — новые шаги строить поверх этого)

v0.24.0 (ВЫПУЩЕН 2026-09-16): **Шаг 6 Персоны + 6-F + 6-G** — `persons`/`person_identities` (UUID), ролевые ссылки `tasks.requestor_id/assignee_id` + `task_comments.author_person_id`, авто-создание при первом контакте + авто-assign при закрытии, UI «Персоны» (вкл. ручное подтверждение), deep-link `?requestor_id=` — квитанции: `archive/PLAN.v0.24.md`.
v0.23.0 (блок закрыт, 2026-09-15, полный текст в `archive/PLAN.v0.23.md`):
входящий поток IMAP + MIME-парсинг + вложения (extraction) · ws-подписки · история статусов (`task_status_history`, `SetTaskStatus`) · копирование/MD-рендер/автолинкинг комментариев · bulk actions (статус/проект, `PATCH /api/tasks`, WS `batch_update`) · **AI-вердикт = только саммари; спойлер с НОВОЙ ЧАСТЬЮ письма — в саммари `user`; вложения legacy user-саммари остаются** · AI-очереди retry 1м/5м/15м/1ч.
v0.22.0: проекты/модули, срез Plane, outbound SMTP, темы, observability — `archive/PLAN.v0.22-...md`.

## Отдельные решения (зафиксированы, действуют независимо от шагов)

1. **Хранение (`attachments`) — не трогаем** (решение владельца, 15.09; баги legacy — только отдельными шагами/багами).
2. **AI-вердикт — только саммари**; спойлер письма — в саммари `user` (решение 15.09, закрыт шагом 5).
3. **Патч-версии = только hotfix.** Фиче-блок = **только MINOR**: v0.22.1 не создавалась (фиксы вошли в v0.23.0).
4. **PR с fix по багам — допустим** (в рамках шага: fix-коммит → green CI → merge → план/CHANGELOG).
5. **Правило 8:** тег делает владелец, но **PR-мерж + CHANGELOG/план — агент** (расширено 15.09: PR #27 = PR #26 + «тег нет» → PR #27).
6. **Правило 10:** строго один шаг; fix — только по багам, не «заодно» (решение владельца 2026-09-13: «1 шаг = 1 цель + фиксы по багу/ревью, не более»).

## Закрытые (квитанции — в архиве)
- **v0.24 — блок «Персоны» (6 + 6-F + 6-G):** — полный текст (квитанции, решения, B-1/B-2): `archive/PLAN.v0.24.md`
- **v0.23 — шаги 0–5:** ws-reconnect · вложения (CAS+API) · история статусов · копирование/MD/автолинки · bulk · AI-вердикт = саммари — полный текст с дискуссионными пометками: `archive/PLAN.v0.23.md`
> **Нумерация** — сквозная: шаги 0–5 = v0.23 (архив). v0.24 = блок «Персоны»: 6 + 6-F + **6-G**. **Модель (FIX 2026-09-17): шаг = своя версия** — v0.25.0 = 7a+7b+7c+6-H (тег 2026-09-17); v0.26.0 = 7d ✅; v0.27.0 = шаг 8 «Версия в UI» ✅; v0.27.1 = hotfix сортировки ✅; v0.27.2 = «Оригинал письма» ✅ (в проде 2026-09-18); хотфикс v0.27.3 = «цитата (quote) в AI-саммари» (открыт, курсор). Патчи `vX.Y.Z+1` — только hotfix (CONTRIBUTING §«Хотфикс»).

## Открытые шаги


### [x] Хотфикс v0.27.2 — «Оригинал письма» (detail) и бейдж «AI-саммари» вернулись (режим C) ✅ 2026-09-18 — квитанция: `archive/PLAN.v0.27-hotfixes.md` (PR #67, squash `06941b5`; тег `v0.27.2` → `51a8273`, в ПРОДЕ; визуал владельца пройден 2026-09-18)

### [x] Хотфикс v0.27.3 — цитата (quote) из письма не отображается в AI-саммари (режим C, согласован 2026-09-18) — **ВЫПУЩЕН 2026-09-19 (тег `v0.27.3` → `218d6e9` + Release + бинарник ✅)**

**Контекст (из записей, верифицировано):**
- Спроектировано в `archive/PLAN.v0.23.md:161`: «`quote` — дословные 1–2 строки из письма-ответа… рендерится blockquote **над саммари** (подпись «Затронуто в письме`). `body` остаётся чистым саммари → копирование MD/TXT не ломается».
- Финальное решение 15.09 (CHANGELOG [0.23.0]) убрало quote только из `ai_verdict`. Промпт-контракт quote сохранён (`internal/ai/orchestrator.go`: «дословные 1–3 строки из ТЕКУЩЕГО письма… Если фрагмент выделить нельзя — quote = `""`») → модель кладёт quote в `verdict_json` и саммари-комментария.
- Реальные записи (задача 416): `task_comments.id 226/230` — `kind='user_comment'`, `verdict_json={"quote":"…"}`; дубли — `ai_verdict` 227/231 с тем же quote.
- UI (`CommentList.vue`): quote сейчас нигде не читается (только CSS markdown-blockquote).

**Иерархия (по решению владельца 2026-09-18):** саммари (своими словами, сразу на виду) → **цитата дословно, подкреплает саммари** → «Оригинал письма» — под спойлером, когда нужен полный контекст.

- **1. Что делаем (вариант 1, согласован; только фронтенд):**
  - В комментарии-саммари (предикат `isAIUserSummary()`), **между бейджем «AI-саммари» и body** — блок: подпись `Затронуто в письме` + quote (визуал: левая рамка/italics, как markdown-blockquote; точные стили — при реализации по существующему CSS-набору).
  - **Источник quote (правило дубля, решение владельца — «только из user_comment ИЛИ из ai_verdict, они дублируют»):** показываем из **саммари-комментария (`user_comment`)** — его собственный `verdict_json.quote`; если он пуст — fallback на `ai_verdict` того же письма (связь `inbox_item_id`) с тем же `quote`; если оба пусты/отсутствуют — **блок не рендерится вообще**.
  - **Edge-cases (согласован):**
    - legacy-саммари `author='user'` (до 15.09, quote в `verdict_json` не пишется) → блок отсутствует;
    - `quote: ""` / поле отсутствует / `verdict_json` невалидный JSON → молча пропускаем (try/catch, без логов-шума в UI);
    - `ai_verdict`-комментарии — quote в них НЕ показываем (решение 15.09: вердикт = только саммари);
    - `body` комментария и экспорт MD/TXT — **не трогаем** (requirement: quote не должен дублироваться в саммари).
  - Бэкенд, API, схема, данные, промпт — **не меняются** (данные уже в `verdict_json`).
- **2. Тесты (RED → GREEN), `frontend/tests/components/CommentList.spec.js`:**
  - RED: «саммари `user_comment` с `verdict_json.quote` → блок «Затронуто в письме» + текст quote виден, при этом `ai_verdict` дубль quote НЕ показывает» (сегодня FAIL → после фикса PASS);
  - fallback: `user_comment` без quote + `ai_verdict` того же `inbox_item_id` с quote → блок виден;
  - пусто: `quote: ""` / нет `verdict_json` / malformed JSON → блок отсутствует (без ошибок);
  - legacy `author='user'` (без quote) → блок отсутствует, деталь и бейдж — как есть (регрессия v0.27.2 не ломается).
  - `make lint && make test` + `npm test` + CI green. e2e — не требуется (компонент), unit + визуал владельца достаточно.
- **3. Приёмка (визуал владельца на `192.168.1.71:8081`, после `./scripts/dev-stack.sh` — make build + make run, порт из `configs/config.env`; инцидент dev-статика, НЕ `run-dev`):**
  - Задача 416: под саммари «Стоимость подтверждена…» — блок «Затронуто в письме» + quote «разработка будет бесплатная…» и «Ок» (второй саммари); бейдж «AI-саммари» и detail «Оригинал письма» — как уже есть (регрессия v0.27.2); `ai_verdict` 227/231 — блока quote нет.
  - Задача 378 (legacy, `author='user'`) — блока quote нет, всё остальное как раньше.
- **4. Порядок (регламент хотфикса, аналог v0.27.2):**
  1. `git tag --sort=-v:refname | head` → последний релиз `v0.27.2`; ветка `hotfix/quote-summary` от `v0.27.2`.
  2. Реализация: `CommentList.vue` (+стили) + unit-кейсы (RED→GREEN) → локально `make lint && make test`, `npm test`.
  3. **PR** на `main`: Scope/Изменения/Тесты (ссылка на эту заготовку) + буллит `### Added` в CHANGELOG `[0.27.3]` — **в том же PR**.
  4. **CI green (Lint+Test)** → squash-merge, ветка удалена.
  5. **Тройка:** отчёт о закрытии → **визуал владельца `192.168.1.71:8081`** (п.3, после `./scripts/dev-stack.sh`) → **явное согласие владельца**.
  6. **Релиз:** явная команда владельца «тег v0.27.3» → тег → workflow Release → проверка бинарника `mailbridge 0.27.3 (commit …)`.
  7. **Деплой :8080** — по явной команде владельца: `~/apps/mailbridge/deploy-released.sh v0.27.3` (health-гейт + `/api/version`, авто-откат; журнал `deploy.log`).
  8. **Закрытие:** курсор → «Сегодня/спринты» (v0.28.0+); B-1 — фоновое.

**Реализация (квитанция-закрытие, 2026-09-19):**
- Ветка `hotfix/quote-summary` от `v0.27.2` (+ merge `main` для `plan-gate.sh` PR #69). Изменены ТОЛЬКО 2 файла фронта: `frontend/src/components/CommentList.vue` (блок quote + функции `parseQuote`/`summaryQuote` + стили `.comment-quote*`) + `frontend/tests/components/CommentList.spec.js` (8 кейсов, RED→GREEN).
- По квитанции (в.1, в.2): свой `verdict_json.quote` → fallback `ai_verdict` того же `inbox_item_id`; `ai_verdict`/legacy/пусто/битый JSON → блока нет (try/catch); `body`/экспорт MD/TXT — не тронуты. Личный баг-фикс в TESTS-файле НЕ вносился.
- Тесты: 8 новых кейсов (RED: 3 failed → GREEN) → CommentList 27/27 PASS; `make lint` 0 issues; `make test` PASS.
- **Фикс тест-флейка (тот же релиз, PR следует):** `TaskTableDue.spec.js > расцветка по due_date` — строка 2 строила «сегодня» как **UTC-дату** (`toISOString()`), а `dueClassOf` считает **локальный** день → тест падал каждое утро 00:00–03:00 МСК (сегодня упал в ~01:29 МСК, на чистом `v0.27.2` так же; CI на UTC не ловит). Фикс: «сегодня» = `todayKey()` из `src/utils/due-date.js` (один источник в компоненте и тесте); доказано: старый код падает в МСК-окне → новый зелёный в МСК и UTC.
- Приёмка: визуал владельца «визуал, ок» (02:0x МСК) + команда «выпускай тег» → **тег `v0.27.3` → `218d6e9` выпущен 2026-09-19, бинарник `0.27.3 (commit: 218d6e9)` проверен из Release-артефакта**. Деплой :8080 — явная команда владельца `deploy-released.sh v0.27.3`.

### [x] Шаг 7a + 7b + 7c + 6-H — Due date (v0.25.0) ✅ 2026-09-17 (7a PR #41, 7b PR #42, 7c PR #44 + PR #46, **6-H хотфикс PR #48**: self-heal identity + raw-автор парсится; визуальный чек владельца: задача 383 «Петрова» — человек или дубль) — квитанции: `archive/PLAN.v0.25.md`; живая «карта» — §«Что уже работает».
### [x] Шаг 7d — Due date: вкладка «Все» + сортировки + фильтры по срокам (v0.26.0) ✅ 2026-09-17 — РЕЖИМ A: вкладка «Все» (дефолт — по активности) + селектор «по срокам / по активности» + 7 серверных фильтров `?due=` + бампит `updated_at` в `AddTaskComment` + e2e `due-filters-7d.spec.js` — квитанции: `archive/PLAN.v0.25.md` (секция «Шаг 7d»); CHANGELOG [0.26.0].


- Отложено (SLA / push/WS / `due_origin` / авто-принятие AI) — это **решения владельца, не идеи** — теперь в **`ROADMAP.md` → «Отложено»** (детали: `archive/PLAN.v0.25.md`).
> **Последовательность** (обновлено 09-17): 7a → 7b → 7c ✅ → 6-H ✅ (v0.25.0) → **7d ✅ (v0.26.0)** → **шаг 8 (v0.27.0+)**. Каждый = 1 цель / 1 PR / 1 CI / 1 merge / курсор на следующий (новая сессия). **Модель: шаг = своя версия** (тег `0.x.y` после каждого закрытого шага; hotfix — патч от последнего тега).

### [x] Шаг 8 — Версия в UI (v0.27.0) ✅ ВЫПУЩЕН 2026-09-18 — квитанция: `archive/PLAN.v0.27.md` (PR #60, squash `c6edd81`, тег `v0.27.0` → `800a62f`, Release + бинарник `0.27.0 (commit: 800a62f)`)

### [ ] Следующая фича после шага 8 — «Сегодня» / спринты (v0.28.0+)
**Правило (2026-09-17):** в `PLAN.md` — **только одна** фича, детализированная на шаги с открытыми решениями. Фича «Сегодня/спринты» — в `ROADMAP.md` (заявлено + открытые вопросы); её старт = режим B → решение вопросов → занесение в PLAN как следующий шаг.

## Наблюдения и «после v0.24.0» (B-1, B-2) — в фоне, не блокируют очередь

> Источники (детали и дискуссионные пометки) — `archive/PLAN.v0.24.md` (секция «Открытые вопросы после 6-F»).

1. **B-1 — Наблюдение на проде (наблюдательный пункт, НЕ кодовый).** После тега v0.24.0: 2–4 недели живых писем на новой сборке. Метрики: динамика `confirmed=false` (кол-во/темп), дубли (один email → N персон), no-reply vs живые, частота ручных подтверждений (появится c 6-G). Сбор — SQL-срезы по `persons`/`person_identities` (квитанция 6-F). Владелец решает сроки остановки накопления.
2. **(C-1) — закрыто в этом блоке как шаг 6-G** (см. отдельный шаг: ручное подтверждение в UI, v0.24, РЕЖИМ A). Не «открытый вопрос» — решение зафиксировано, ждёт старта.
3. **B-2 — Правило авто-подтверждения: ОТКРЫТО до B-1.** Кандидаты (обсуждение 2026-09-16): «ручное подтверждение закрывает вопрос (6-G)»; «N писем + стабильное имя»; «merge подтверждает целевую персону». **Решение — только по данным B-1. Пока открыт: не в код, не в шаги.**

> **Порядок (обновлено 2026-09-17):** 6-F ✅ → 6-G ✅ (PR #35) → тег v0.24.0 ✅ → 7a ✅ (PR #41) → 7b ✅ (PR #42) → 7c ✅ (PR #44 + PR #46) → 6-H ✅ (PR #48) → **тег v0.25.0 (2026-09-17)** → **7d ✅ (v0.26.0)** → **курсор: шаг 8 / «Следующая фича» (v0.27.0+)**. **B-1** — фоновое наблюдение на проде (2–4 нед), **не блокирует очередь**; **B-2** (правило авто-подтверждения) — только по данным B-1, отдельной мини-фичей (решается по данным).

## Что НЕ делаем (границы, чтобы не предлагать повторно)
- «Починить storage» отдельным планом — НЕ делаем (решение владельца; баги legacy только отдельными шагами/багами)
- Переписывать legacy AI-саммари (`author='user'`, `ai_processed=-1`) — НЕ делаем; новые вердикты — только по шагу 5
- Дубли письма (dedup) в цепочке — НЕ делаем (решение владельца: дубли ок)
- Plane как бэкенд задач — НЕ делаем (удалён в v0.22, ADR-0001)
- v1.0.0 сейчас — НЕ делаем (v1.0.0 далеко — v0.x-циклы, см. v0.23-блок)

## Выпущено (индекс, одна строка на версию)
- **v0.27.2 (2026-09-18)** — hotfix: «Оригинал письма» (detail) + бейдж «AI-саммари» вернулись на новые саммари-комментарии (PR #67, squash `06941b5`). Тег `v0.27.2` → `51a8273`, бинарник `0.27.2 (commit: 51a8273)`; В ПРОДЕ (деплой 23:06). Квитанция: `archive/PLAN.v0.27-hotfixes.md`. Дальше: **v0.27.3 (цитата) → «Сегодня/спринты» (v0.28.0+)**.
- **v0.27.1 (2026-09-18)** — hotfix: сортировка «По срокам» — задачи без срока: новые наверху (`created_at DESC`); PR #63, тег `v0.27.1` → `6e14617`, бинарник в Release; был в проде до v0.27.2. Квитанция: `archive/PLAN.v0.27-hotfixes.md`.
- **v0.27.0 (2026-09-18)** — шаг 8 «Версия в UI»: публичный `GET /api/version` + бейдж версии в шапке Dashboard (нативный title-tooltip: commit+built), `make run-dev` вшивает ldflags — тег → Release, бинарник `0.27.0 (commit: 800a62f)`; квитанция `archive/PLAN.v0.27.md`.
- **v0.26.0 (2026-09-17)** — шаг 7d: Due date, вкладка «Все» + сортировки + фильтры `?due=` (7 значений) + бампит `updated_at` в `AddTaskComment` + e2e `due-filters-7d` — тег → release pipeline. Дальше: **v0.27.0 ✅ (2026-09-18)**.
- **v0.25.0 (2026-09-17)** — шаг 7 (Due date: 7a ядро+API → 7b AI-предложение → 7c подтверждение UI + фикс календаря PR #46) + **хотфикс 6-H (PR #48: self-heal identity crash-loop + raw-автор парсится)** ВЫПУЩЕН: тег → Release, бинарник 0.25.0. Дальше: **7d ✅ (v0.26.0)** → **шаг 8 (v0.27.0+)**.
- **v0.24.0 (2026-09-16)** — блок «Персоны» (6 + 6-F + 6-G) ВЫПУЩЕН: тег → Release, бинарник 0.24.0. Дальше: B-1 наблюдение (2–4 нед) → B-2 → v0.25+.
- **v0.23.0 (2026-09-15)** — шаги 0–5: ws · вложения (extraction) · история статусов · MD-карта · bulk · вердикты (только саммари) → `archive/PLAN.v0.23.md`, CHANGELOG [0.23.0]
- **v0.22.0 (2026-08-30)** — проекты/модули, Plane-срез, outbound, темы → `archive/PLAN.v0.22-projects-modules.md`, CHANGELOG
- **v0.21.0 / v20–v17** — helpdesk + AI → `archive/PLAN.v17-21.md`; **v0–16** (Plane-based) → `archive/PLAN.v0-plane-era.md`

## Методология
→ `CONTRIBUTING.md` раздел «Методология разработки» (режимы A/B/C, уровни «шаг»/«версия», чек-листы СТАРТА/ЗАКРЫТИЯ/ВЫПУСКА, стоп-гейт, нумерация, передача между сессиями).

# Mailbridge — план (курсор)

> **Одна ответственность (SRP, зафиксирован 2026-09-19):** этот файл — **только курс**: где стоим и какой следующий шаг. Рецепты (режимы, хотфикс, роли) — `CONTRIBUTING.md` · «зачем/границы фич» и фон — `ROADMAP.md` · факты (квитанции, версии) — `CHANGELOG.md` + `archive/PLAN.v*.md` · границы «что не делаем» — `ROADMAP.md`.
> **Схема v0.23**: в живом плане — только открытые шаги; выпущенные версии — **одна строка** в «Выпущено», квитанции — в `archive/`.
> **Рестарт сессии (промпт)**: «план `site/mailbridge/PLAN.md`, skill `mailbridge-dev`; с какого шага читать — „Статус“ в шапке; чек-лист СТАРТА». **Первое действие любой сессии — `plan-gate.sh start`.**

## Статус (курсор — читать первым)
**2026-09-20.** **v0.28.0 — ВЫПУЩЕН** (тег → tip `main`, Release + бинарник; тройка закрыта; прод :8080 = v0.28.0). Issue #75 — ЗАКРЫТО (шаг 0 — PR #83; шаг 1 — PR #84, оба merged). **КУРСОР:** шаг **29a** (issue #81 «По дате создания», кандидат v0.29.0) — реализован + проверен (Go/vitest/lint green, e2e 48/48), ветка → PR → merge.

## Открытые шаги

### [ ] 29a — «По дате создания»: новый вариант сортировки + НОВЫЙ ДЕФОЛТ (из issue #81, кандидат v0.29.0) — В РАБОТЕ 2026-09-20

**Решения (issue #81, закрыто 2026-09-20):** «По дате создания» (`?sort=created`, новые сверху — `created_at DESC`) — **первый пункт «Сортировки» и дефолт** (был «По срокам»); смена вкладки сбрасывает сортировку на «По дате создания»; невалидное `?sort=` — `400` (как `?plan=`/`?due=`; молчаливый fallback скрывал бы typo). **Границы:** «По срокам»/«По активности» не переименовываются (2-й/3-й пункты); endpoint `/api/projects/{id}/tasks` без явного sort получает новый дефолт (свежие сверху — принято; решение владельца 2026-09-20). **Тесты:** Go `TestListTasks_ListSortCreatedDefault` (дефолт = новые сверху) + `TestListTasks_ListSortDueExplicit` (явный due; тик-брейкер v0.27.1 сохранён) + `TestAPI_ListTasksSort` (200: due/updated/created; 400: bogus/CREATED/asc); unit `tasks-tabs.spec.js` (сброс на created) + `DashboardTasksTab.spec.js`; e2e `sort-created-29.spec.js` (default-order, 400-on-junk, UI-first-item) + синка `due-filters-7d.spec.js` (явный `sort=due` — 7a-сценарий). Доки: `docs/api.md` § sort. **Прогон:** Go 76/76 (sqlite 34 + web 42), vitest 184/184, `golangci-lint` 0 (unparam-фикс `mk`), `eslint --max-warnings=0` clean, e2e 48/48 (детерминизм ×N — до merge); dev-стек rebuild'нут на новой статике. **Блокеры:** нет; после merge — тройка (отчёт + визуал владельца :8081 + явная команда) → тег v0.29.0 → деплой по команде.

## Выпущено (индекс — **макс 5 последних** + строка-указатель на старое; одна строка на версию, квитанции в `archive/`, CHANGELOG)
- **v0.28.0 (2026-09-20)** — «План»: `scheduled_date` + API `?plan=all|none|today|tomorrow|week` (+просроченные due) + вкладки-дропдауны «Статус»/«План» (таб = пункт выбора) + «Назад» возвращает выбор вкладки (PR #72–#79). Квитанция: `archive/PLAN.v0.28.md`.
- **v0.27.3 (2026-09-19)** — hotfix: цитата (quote) из письма в AI-саммари (PR #70, squash `8e6f493`) + фикс тест-флейка TaskTableDue (PR #71, `todayKey()`). В ПРОДЕ. Квитанция: `archive/PLAN.v0.27-hotfixes.md`.
- **v0.27.2 (2026-09-18)** — hotfix: «Оригинал письма» + бейдж «AI-саммари» на новых саммари-комментариях (PR #67, squash `06941b5`). Квитанция: `archive/PLAN.v0.27-hotfixes.md`.
- **v0.27.1 (2026-09-18)** — hotfix: сортировка «По срокам» — без срока: новые наверху (PR #63). Квитанция: `archive/PLAN.v0.27-hotfixes.md`.
- **v0.26.0 (2026-09-17)** — 7d: вкладка «Все» + сортировки + фильтры `?due=` (7 значений). Квитанция: `archive/PLAN.v0.25.md`.
- **v0.27.0 и v0.25.0 и раньше (v0.24 Персоны · v0.23 шаги 0–5 · v0.22 · v0.21 · v0–16)** — полные квитанции: `CHANGELOG.md` + `archive/PLAN.v*.md`.

**Методология** → `CONTRIBUTING.md` (режимы, уровни, хотфикс, чек-листы, стоп-гейт, передача между сессиями).

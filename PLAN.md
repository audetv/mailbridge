# Mailbridge — план (курсор)

> **Одна ответственность (SRP, зафиксирован 2026-09-19):** этот файл — **только курс**: где стоим и какой следующий шаг. Рецепты (режимы, хотфикс, роли) — `CONTRIBUTING.md` · «зачем/границы фич» и фон — `ROADMAP.md` · факты (квитанции, версии) — `CHANGELOG.md` + `archive/PLAN.v*.md` · границы «что не делаем» — `ROADMAP.md`.
> **Схема v0.23**: в живом плане — только открытые шаги; выпущенные версии — **одна строка** в «Выпущено», квитанции — в `archive/`.
> **Рестарт сессии (промпт)**: «план `site/mailbridge/PLAN.md`, skill `mailbridge-dev`; с какого шага читать — „Статус“ в шапке; чек-лист СТАРТА». **Первое действие любой сессии — `plan-gate.sh start`.**

## Статус (курсор — читать первым)
**2026-09-20.** **v0.27.3 — ВЫПУЩЕН, В ПРОДЕ** (02:09 МСК, health 200). **v0.28.0: шаги 28a+28b+28c+28d+28e ЗАКРЫТЫ** (28e: вкладки-дропдауны «Статус»/«План» — таб = пункт выбора, Все/Без плана/Сегодня/Завтра/Неделя; отдельный селект — отменён решением владельца 2026-09-20) — **v0.28.0 СБОРАН**; тег + деплой — по явному слову владельца.

## Открытые шаги

### [ ] v0.28.0 — «План»: scheduled_date + вкладка «План» + реорганизация вкладок
**Решения владельца (режим B, 2026-09-19 — принято):** две даты у задачи: `due_date` (обещание) + `scheduled_date` (план, когда делаю). «Сегодня» = scheduled сегодня **+ просроченные due** (крайний сценарий «совсем не видеть просроченные» — отклонён). Вкладки: **Инбокс, Статус, План, Проекты, Персоны** — «Статус» и «План» — **вкладки-дропдауны** (решение владельца 2026-09-20, аналогия Bootstrap «Tabs with dropdowns»: таб = дропдаун-кнопка, пункт выбора, таб показывает выбранный; отдельный селект — НЕ нужен): «Статус» — Все/Активные/Бэклог/Выполненные/Закрытые; «План» — Все/Без плана/Сегодня/Завтра/Неделя. Состояние в URL `?tab=...` (+ `?status=`, `?plan=`); старое `?tab=active`/`backlog`… — **ломается** (закладок нет, 1 пользователь). **Границы (ROADMAP):** НЕ спринты, НЕ SLA, НЕ «моё»/assignee, НЕ AI-извлечение scheduled; статус задачи при «План» не меняется.
**Шаги (каждый = 1 цель / 1 PR / 1 CI / 1 merge / новая сессия):**
1. **28a — `scheduled_date` (БД + API): ЗАКРЫТ 2026-09-19 (PR #72).** Колонка `tasks.scheduled_date` (TEXT NULL, идемпотентная миграция) + индекс `idx_tasks_scheduled_date` (под 28b). API: `POST /api/tasks` принимает `scheduled_date`; `PATCH /api/tasks/{id}` — установка (строка) / снятие (`null`); валидация — те же правила, что `due_date` (канон `YYYY-MM-DD`, `400` на мусор: `DD.MM.YYYY`, `2026-02-31`, время); `tasks.list` отдаёт поле. Рефакторинг: `validDueDate` → `validTaskDate` (общий валидатор двух дат). Тесты: 4 sqlite-теста `TestScheduledDate_*` (RED→GREEN) + 3 API-теста (`UpdateTaskScheduledDate` вкл. «scheduled не трогает due», `RejectsBadFormat` 5 кейсов, `CreateTaskWithScheduledDate`). Доки: `docs/api.md` (POST/PATCH/GET + примеры), `docs/data-model.md` (колонка + индекс), CHANGELOG [0.28.0]. **Квитанция-строка закрыта.**
2. **28b — API-срез «План»: ЗАКРЫТ 2026-09-19 (PR #73).** `GET /api/tasks?plan=today|tomorrow|week` — окно scheduled (today = H, tomorrow = H+1, week = H..H+6) + ВСЕГДА просроченные due (`due_date < H`, активные статусы new/in_progress/backlog; completed/closed вне плана; решение владельца 2026-09-19). `H` = локальный день сервера (как `?due=`). Валидация `plan` (400 на мусор, как 7d). Тесты: `TestPlan_FilterByPlanBuckets` (12 задач, все ветки) + `TestAPI_ListTasksPlan` (RED→GREEN). Доки: `docs/api.md` (query-параметры), CHANGELOG [0.28.0]. **Квитанция-строка закрыта.**
3. **28c — UI «scheduled» в карточке + списке: ЗАКРЫТ 2026-09-19 (PR #74).** Деталька — поле «План» (DatePicker, установка → `PATCH {scheduled_date}`, снять → `null`; due не трогается — независимые поля); список — колонка «План» (тег с датой; тон другой, не «дедлайн»: план сегодня — `info`, дальше — `secondary`). Тесты: unit `TaskDetailScheduled.spec.js` (предзаполнение, PATCH, снятие, due не трогается) + `TaskTableScheduled.spec.js` (заголовки/теги/«—»/тон) + e2e `scheduled-date-28c.spec.js` (2 сценария владельца из режима B, кликами/записью в DatePicker, API-подтверждение итога). Пrettier/eslint/`vitest` (158 pass) + e2e-своих 3 pass (2 провала в полном прогоне — `bulk-actions` + `projects-to-tasks C` — предсуществующие: стабильно падают на чистом main в этом окружении — не моя регрессия, baseline подтверждён). Доки: CHANGELOG [0.28.0]. **Квитанция-строка закрыта.**
4. **28d — Вкладки: «Статус» + селект / «План» + селект / порядок (ЗАКРЫТ 2026-09-20, бранч `feat/step28d-tabs` → PR).** 5 статусных вкладок → «Статус» + селект (Все/Активные/Бэклог/Выполненные/Закрытые → прежние наборы статусов) + «План» + селект (Сегодня/Завтра/Неделя = `?plan=today|tomorrow|week`, срез 28b); порядок Лента→Статус→План→Проекты→Персоны; URL `?tab=`+`?status=`/`?plan=` — source of truth (внешний параметр при смене вкладки — `router.replace`), fallback неизвестного `?tab`/мусорного `?status=`/`?plan=` — «Статус»/«Все» и «План»/«Сегодня», старые ключи ломаются (без миграции). Тесты: RED→GREEN `tests/stores/tasks-tabs.spec.js` (стор `STATUS_FILTER`/`PLAN_FILTER`, `setStatusFilter`/`setPlanFilter`, fetch params) + `tests/views/DashboardTasksTab.spec.js` (11: порядок 5 вкладок, дефолт, deep-link `?tab=status&status=completed`/`?tab=plan&plan=tomorrow`, мусор `?status=`/`?plan=`, смена вкладки сбрасывает внешний параметр, «Создать задачу» в обеих, рендер по вкладкам) + e2e `auth.spec.js` (5 вкладок, both deep-links, **reload переживает** `?tab=plan&plan=today`); синка `projects-to-tasks`/`persons`/`due-filters-7d`/`websocket-reconnect` на новую схему. Полные проверки: `npm run lint` (0 errors), `vitest` 175 pass, e2e-фулл 44 pass / 1 fail — **предсуществующий** `bulk-actions` (issue #75, baseline 28c: падает и на чистом main), `go test -race ./...` ok, golangci-lint 0 issues, `npm run build` ok. Доки: `docs/api.md` (§ UI, 28d), CHANGELOG [0.28.0]. **Квитанция-строка закрыта.**
5. **28e — Вкладки: селект-фильтры → вкладки-дропдауны (ЗАКРЫТ 2026-09-20, ветка `feat/step28e-dropdown-tabs` → PR).** Финальное решение владельца 2026-09-20 (аналог Bootstrap «Tabs with dropdowns»): «Статус» и «План» — вкладки-дропдауны (таб = пункт выбора, выбранный пункт показан на табе; отдельный селект-фильтр убран — 28d UI откатён). Пункты: Статус — Все/Активные/Бэклог/Выполненные/Закрытые; План — **Все/Без плана**/Сегодня/Завтра/Неделя («Все/Без плана» — добавление по решению владельца 2026-09-20). API: `?plan=none` (`scheduled_date IS NULL`; просроченные due туда НЕ добавляются), `?plan=all`/(пусто) — без фильтра. URL: `?tab=status&status=<val>` / `?tab=plan&plan=<val>`, дефолты «Активные»/«Сегодня», переживает reload; смена вкладки сбрасывает чужой параметр (`router.replace`). Тесты: store `tasks-tabs.spec.js` (28e-набор all/none/today/tomorrow/week; all → `plan` не в params), view `DashboardTasksTab.spec.js` (таб показывает выбранный пункт; выбор пункта активирует вкладку; deep-link + мусор → дефолты; «Создать задачу» в обеих) + синка 12 e2e-мест; Go: `TestPlan_FilterByPlanBuckets` (+ветка `none`) и `TestAPI_ListTasksPlan` (`none`/`all` → 200). Доки: `docs/api.md` (§ UI 28d/28e, `?plan=` + none/all), `ROADMAP.md`, CHANGELOG [0.28.0]. **Квитанция-строка закрыта.**
**Курсор после 28e:** v0.28.0 собран; тег + деплой — по явному слову владельца (тройка: отчёт → визуал :8081 → явка). **После v0.28.0** — расследовать 2 падающих e2e-теста в dev-окружении (`bulk-actions` + `projects-to-tasks C`) — **issue #75** (ROADMAP.md «Здоровье e2e»), решение владельца 2026-09-19.

## Выпущено (индекс — **макс 5 последних** + строка-указатель на старое; одна строка на версию, квитанции в `archive/`, CHANGELOG)
- **v0.27.3 (2026-09-19)** — hotfix: цитата (quote) из письма в AI-саммари (PR #70, squash `8e6f493`) + фикс тест-флейка TaskTableDue (PR #71, `todayKey()`). В ПРОДЕ. Квитанция: `archive/PLAN.v0.27-hotfixes.md`.
- **v0.27.2 (2026-09-18)** — hotfix: «Оригинал письма» + бейдж «AI-саммари» на новых саммари-комментариях (PR #67, squash `06941b5`). Квитанция: `archive/PLAN.v0.27-hotfixes.md`.
- **v0.27.1 (2026-09-18)** — hotfix: сортировка «По срокам» — без срока: новые наверху (PR #63). Квитанция: `archive/PLAN.v0.27-hotfixes.md`.
- **v0.27.0 (2026-09-18)** — шаг 8 «Версия в UI»: `GET /api/version` + бейдж версии в шапке (PR #60). Квитанция: `archive/PLAN.v0.27.md`.
- **v0.26.0 (2026-09-17)** — 7d: вкладка «Все» + сортировки + фильтры `?due=` (7 значений). Квитанция: `archive/PLAN.v0.25.md`.
- **v0.25.0 и раньше (v0.24 Персоны · v0.23 шаги 0–5 · v0.22 · v0.21 · v0–16)** — полные квитанции: `CHANGELOG.md` + `archive/PLAN.v*.md`.

**Методология** → `CONTRIBUTING.md` (режимы, уровни, хотфикс, чек-листы, стоп-гейт, передача между сессиями).

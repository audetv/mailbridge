# Mailbridge — план (текущий)

> **Методология** — `CONTRIBUTING.md`, раздел «Методология разработки» (режимы, уровни, чек-листы, стоп-гейт). Этот файл — только курс: где стоим и что дальше.
> **Схема v0.23**: живой план = только открытые шаги. Выпущенные/завершённые блоки уходят в `archive/`, в этом файле — **одна строка** в «Выпущено».
> **Рестарт сессии (промпт)**: «план `site/mailbridge/PLAN.md`, skill `mailbridge-dev`; с какого шага читать — „Статус“ в шапке; чек-лист СТАРТА».

## Статус (курсор — читать первым)

**Дата обновления:** 2026-09-16 (v0.24.0 выпущен; **шаг 7a закрыт** ✅ — срок задач: поля + API; курсор → **7b**, РЕЖИМ A; B-1 — фоновое наблюдение)
**Версия:** **v0.24.0 — ВЫПУЩЕН 2026-09-16** (тег `v0.24.0` → Release; блок «Персоны» = шаги 6 (✅) + 6-F (✅) + 6-G (✅)). Следующая минорная — **v0.25 = шаг 7 (Due date: 7a→7b→7c→7d) + шаг 8**.
**Текущий шаг:** **шаг 7b (Due date: AI-предложение срока) — РЕЖИМ A, курсор** (решения шага — в шапке блока 7a; 7a ✅ закрыт 2026-09-16, ветка `feat/step7a-due-date-core`). Каждый подшаг: 1 цель, 1 PR, 1 CI green, 1 merge, курсор на следующий (новая сессия). **B-1** (наблюдение на проде 2–4 нед по метрикам confirmed) идёт **в фоне и НЕ блокирует ни один кодовый шаг**; **B-2** (правило авто-подтверждения) — отдельная мини-фича, только по данным B-1, не в очереди перед шагом 7.

## Что уже работает (карта — новые шаги строить поверх этого)

v0.24.0 (ВЫПУЩЕН 2026-09-16): **Шаг 6 Персоны + 6-F + 6-G** — `persons`/`person_identities` (UUID), ролевые ссылки `tasks.requestor_id/assignee_id` + `task_comments.author_person_id`, авто-создание при первом контакте + авто-assign при закрытии, UI «Персоны» (вкл. ручное подтверждение), deep-link `?requestor_id=` — квитанции: `archive/PLAN.v0.24.md`.
v0.23.0 (блок закрыт, 2026-09-15, полный текст в `archive/PLAN.v0.23.md`):
входящий поток IMAP + MIME-парсинг + вложения (extraction) · ws-подписки · история статусов (`task_status_history`, `SetTaskStatus`) · копирование/MD-рендер/автолинкинг комментариев · bulk actions (статус/проект, `PATCH /api/tasks`, WS `batch_update`) · **AI-вердикт = только саммари; спойлер с НОВОЙ ЧАСТЬЮ письма — в саммари `user`; вложения legacy user-саммари остаются** · AI-очереди retry 1м/5м/15м/1ч.
v0.22.0: проекты/модули, срез Plane, outbound SMTP, темы, observability — `archive/PLAN.v0.22-...md`.

## Отдельные решения (зафиксированы, действуют независимо от шагов)

1. **Хранение (`attachments`) — не трогаем** (решение владельца, 15.09; баги legacy — только отдельными шагами/багами).
2. **AI-вердикт — только саммари**; спойлер письма — в саммари `user` (решение 15.09, закрыт шагом 5).
3. **Патч-версии = только hotfix.** Фиче-блок = **только MINOR**: v0.22.1 не создавалась (фиксы вошли в v0.23.0).
4. **PR с fix по багам — допустим** (в рамках шага: fix-коммит → green CI → merge → план/CHANGELOG).
5. **Правило 8:** тег делает владелец, но **PR-мерж + CHANGELOG/план — агент** (расширено 15.09: PR #27 = PR #26 + «тег нет» → PR #27).
6. **Правило 10:** строго один шаг; fix — только по багам, не «заодно» (решение владельца 2026-09-13: «1 шаг = 1 цель + фиксы по багу/ревью, не более»).

## Закрытые (квитанции — в архиве)
- **v0.24 — блок «Персоны» (6 + 6-F + 6-G):** — полный текст (квитанции, решения, B-1/B-2): `archive/PLAN.v0.24.md`
- **v0.23 — шаги 0–5:** ws-reconnect · вложения (CAS+API) · история статусов · копирование/MD/автолинки · bulk · AI-вердикт = саммари — полный текст с дискуссионными пометками: `archive/PLAN.v0.23.md`
> **Нумерация** — сквозная: шаги 0–5 = v0.23 (архив). v0.24 (текущая) = блок «Персоны»: 6 + 6-F + **6-G**. Далее: шаг 7 (Due/SLA) и 8 (Сегодня) — **v0.25** (перенумерация версий — рекомендованный сценарий, на согласовании владельца).

## Открытые шаги

### [x] Шаг 7a — Due date, ядро: поля срока + API (v0.25) ✅ 2026-09-16
**Цель.** Задача знает утверждённый срок (`due_date`) + дата/состояние AI-предложения; срок ставится/сменяется/очищается через API; серверная сортировка. **Без AI-извлечения и без UI** (это 7b/7c).
**Решения (общие для 7a–7d, зафиксированы 2026-09-16, РЕЖИМ B закрыт):**
1. Срок = **дата без времени** (`due_date DATE`); задача без срока — норма, ничто не алертится.
2. AI извлекает **явно обозначенные** сроки («до 18.09», «нужно до…»); нет — `null`.
3. AI **предлагает** (`ai_due_date` + `due_ai_pending`), **человек решает**; до принятия — влияет ни на что.
4. **Ручное всегда приоритетнее**: `due_source='manual'` AI не перезаписывает; «изменить» после AI = manual.
5. `ai_due_date` хранится **ВСЕГДА** (и отклонённые) — база ошибок AI для учёта попадания/промаха.
6. Флаг `MAILBRIDGE_AI_AUTO_APPLY_DUE` default **false** (true = автоприменение, source=`ai`) — по опыту, отложено.
7. Визуал — **только расцветка/бейдж + сортировка**; напоминания/push/WS — отложено.
8. SLA-нормативы — отложено (нужна статистика; опора готова: `is_internal`, `due_date`, база ошибок AI).
9. Вкладка «Все» + селектор «по срокам / по активности» + фильтры по срокам (дефолт: «Все» → активность, статусные → сроки) — всё серверное.
10. «Личное назначение (своё на сегодня/неделю)» = срок на нужную дату + фильтры — **отдельного поля нет**; свой/чужой срок — отложено (`due_origin`).
11. Разбивка: **7a → 7b → 7c → 7d**, каждый = 1 цель / 1 PR / 1 CI / 1 merge (новые сессии).
**План:** 1. Миграция `tasks`: `due_date DATE NULL`, `ai_due_date DATE NULL`, `due_source TEXT NULL CHECK (due_source IN ('ai','manual'))`, `due_ai_pending INTEGER NOT NULL DEFAULT 0` (idempotent, `IF NOT EXISTS`). 2. API: поля в GET списка/детали; постановка/смена/очистка ручного срока (source=`manual`); серверная сортировка `?sort=due|updated` (due: просроченные сверху, без срока внизу; updated: по свежести) — работает через пагинацию. 3. Unit-тесты (store + handler).
**Приёмка:** срок ставится/изменяется/очищается через API, переживает рестарт; сортировка due/updated стабильна через пагинацию.
**Квитанция при закрытии: → `archive/PLAN.v0.25.md` (создать при первом закрытии v0.25) — блок 7a; живой план = одна строка.**
**Выполнено (2026-09-16, ветка `feat/step7a-due-date-core`):** миграция (4 колонки, idempotent guard `columnExists`; тип `TEXT` — тип `DATE` давал аффинити и драйвер перекодил бы `YYYY-MM-DD`→RFC3339, caught тестами); struct `Task` (+4 поля, AI-поля только инфраструктура для 7b) + `TaskFilter.Sort`; CreateTask/PatchTask/scanTask; API: `due_date` в GET/PATCH/POST, `validDueDate` (формат + реальный календарный день), ручная установка → `due_source='manual'`, `?sort=due|updated` (due: ASC, NULL-последними); доки (api.md, data-model.md, CHANGELOG v0.25). Тесты: store `sqlite_test.go` (создание/чтение/обновление/сброс), web `api_duedate_test.go` (create, set/unset, 4 invalid-форматов→400, `due_source=manual`) — `go test ./...` green, `go vet` clean. Приёмка выполнена: срок ставится/сменяется/очищается через API (рестарт-независимо — SQLite-хранение), сортировка due/updated стабильна (серверная, работает через пагинацию).

### [ ] Шаг 7b — Due date: AI-предложение срока (v0.25) — РЕЖИМ A
**Цель.** AI извлекает явно обозначенные сроки из письма и **предлагает** (`ai_due_date` + `due_ai_pending`); человек решает в 7c. **Ручной срок (7a) AI не перезаписывает.**
**План:** 1. Промпт: явно обозначенные сроки («до 18.09», «нужно до…»); в вердикт (new **и** update) `"due_date": "YYYY-MM-DD" | null`; срока нет — null. 2. Логика: AI пишет только `ai_due_date`+pending; при `due_source='manual'` — предложение без применения; `ai_due_date` хранится ВСЕГДА (база ошибок AI: попал/упустил/заврался). 3. Флаг `MAILBRIDGE_AI_AUTO_APPLY_DUE` (default **false** — ждать подтверждения; true = применить сразу, source=`ai`) в config + `config.example.env`. 4. Unit-тесты (схема вердикта, приоритет manual, флаг, хранение всегда).
**Приёмка:** письмо «сделайте до 18.09» → задача получает pending-предложение `ai_due_date`; у задачи с manual-сроком — `due_date` и `due_source` не трогается; флаг по умолчанию `false`.

### [ ] Шаг 7c — Due date: подтверждение в UI (v0.25) — РЕЖИМ A
**Цель.** Полный цикл «AI предложил → человек решил» в UI.
**План:** 1. Карточка задачи: поле даты (поставить/изменить/очистить; любой ручной вход = source=`manual`). 2. Строка: бейдж «срок? (AI: дата)» + **принять / изменить (значение AI предзаполнено) / отклонить**; pending-подсветка строки. 3. Расцветка утверждённого срока: просрочено=красный, сегодня=жёлтый, дальше=нейтральный, без срока=пусто; колонка «Срок» в таблице. 4. Unit (компоненты) + **e2e Playwright** (цикл принять/изменить/отклонить).
**Приёмка:** цикл работает в UI; отклонённое предложение остаётся в `ai_due_date` (база ошибок AI); pending-подсветка исчезает после решения.

### [ ] Шаг 7d — Due date: вкладка «Все» + режимы сортировки + фильтры по срокам (v0.25) — РЕЖИМ A
**Цель.** Оперативное управление потоком: «что на сегодня/на неделе» + «своё» (личное назначение) без отдельных полей.
**План:** 1. Вкладка **«Все»** (задачи любого статуса; дефолт-сортировка «по активности» — ответ на письмо/новый комментарий поднимает задачу наверх). 2. Селектор сортировки **«по срокам / по активности»** (серверные `?sort=` из 7a) — один селектор, дефолт по вкладке: «Все» → по активности; Активные/Бэклог/Выполненные/Закрытые → по срокам. 3. Фильтры по срокам (серверные): просроченные / сегодня / завтра / 7 дней / 30 дней / без срока / **срок на подтверждении** (pending). 4. **e2e Playwright** (вкладка/селектор/фильтры — обязательный триггер).
**Приёмка:** личное «кинул на сегодня» — наверху в «Все»; просроченные — наверху в статусных вкладках; задача с новым комментарием не тонет в «Все».

### Шаги 7a–7d: ОТЛОЖЕНО (зафиксировано 2026-09-16 — записано, чтобы не потерять)
- **SLA-нормативы** — после статистики (опора готова: `is_internal`, `due_date`, база ошибок AI).
- **Напоминания/push/WS** — отдельная фича.
- **`due_origin`** (своё vs клиентское назначение) — только если покажут данные/опыт (пока: срок + фильтры).
- **Авто-принятие AI-сроков** — флаг `MAILBRIDGE_AI_AUTO_APPLY_DUE` (с 7b, default false) — по опыту.
> **Последовательность**: 7a → 7b → 7c → 7d → шаг 8. Каждый = 1 цель / 1 PR / 1 CI / 1 merge / курсор на следующий (новая сессия). Все — в **v0.25**.

### [ ] Шаг 8 — «Сегодня» / спринты: что отвечать сегодня (v0.25 или позже) — РЕЖИМ B
**Цель.** Ответ на вопрос владельца «что отвечать сегодня»: приоритизация (due из шага 7, приоритет AI, возраст, «новый»/«ожидание»).
**Открытые вопросы:** 1. Что считаем «сегодня»: due_date = сегодня + просроченные? (зависит от шага 7) 2. «Спринт» терминологически (как в Plane) vs обычный список? 3. Дашборд-вид: отдельная вкладка / сортировка-фильтры / «моё»? 4. Зависимость от 7d: «сегодня/завтра» как фильтры уже появятся в 7d — шаг 8 = «оперативная вкладка» + приоритеты (due + приоритет AI + возраст).
**Решение:** не зафиксировано (зависит от шага 7).
**План шага:**
*После решения:* 1. UI (вкладка «Сегодня» / приоритеты) 2. API (сортировка по приоритетам) 3. тесты

## Наблюдения и «после v0.24.0» (B-1, B-2) — в фоне, не блокируют очередь

> Источники (детали и дискуссионные пометки) — `archive/PLAN.v0.24.md` (секция «Открытые вопросы после 6-F»).

1. **B-1 — Наблюдение на проде (наблюдательный пункт, НЕ кодовый).** После тега v0.24.0: 2–4 недели живых писем на новой сборке. Метрики: динамика `confirmed=false` (кол-во/темп), дубли (один email → N персон), no-reply vs живые, частота ручных подтверждений (появится c 6-G). Сбор — SQL-срезы по `persons`/`person_identities` (квитанция 6-F). Владелец решает сроки остановки накопления.
2. **(C-1) — закрыто в этом блоке как шаг 6-G** (см. отдельный шаг: ручное подтверждение в UI, v0.24, РЕЖИМ A). Не «открытый вопрос» — решение зафиксировано, ждёт старта.
3. **B-2 — Правило авто-подтверждения: ОТКРЫТО до B-1.** Кандидаты (обсуждение 2026-09-16): «ручное подтверждение закрывает вопрос (6-G)»; «N писем + стабильное имя»; «merge подтверждает целевую персону». **Решение — только по данным B-1. Пока открыт: не в код, не в шаги.**

> **Порядок (ФИКСИРОВАН 2026-09-16, владелец):** 6-F ✅ → 6-G ✅ (PR #35) → тег v0.24.0 ✅ → **шаг 7 (v0.25): РЕЖИМ B закрыт — решения зафиксированы 2026-09-16, курсор → 7a (РЕЖИМ A)** → 7b → 7c → 7d → **шаг 8**. **B-1** — фоновое наблюдение на проде (2–4 нед), **не блокирует очередь**; **B-2** (правило авто-подтверждения) — только по данным B-1, отдельной мини-фичей (патч в v0.25 или отдельным шагом — решается по данным).

## Что НЕ делаем (границы, чтобы не предлагать повторно)
- «Починить storage» отдельным планом — НЕ делаем (решение владельца; баги legacy только отдельными шагами/багами)
- Переписывать legacy AI-саммари (`author='user'`, `ai_processed=-1`) — НЕ делаем; новые вердикты — только по шагу 5
- Дубли письма (dedup) в цепочке — НЕ делаем (решение владельца: дубли ок)
- Plane как бэкенд задач — НЕ делаем (удалён в v0.22, ADR-0001)
- v1.0.0 сейчас — НЕ делаем (v1.0.0 далеко — v0.x-циклы, см. v0.23-блок)

## Выпущено (индекс, одна строка на версию)
- **v0.24.0 (2026-09-16)** — блок «Персоны» (6 + 6-F + 6-G) ВЫПУЩЕН: тег → Release, бинарник 0.24.0. Дальше: B-1 наблюдение (2–4 нед) → B-2 → v0.25.
- **v0.25 (в разработке)** — шаг 7 (Due date: 7a ядро+API → 7b AI-предложение → 7c подтверждение UI → 7d «Все»+сортировка+фильтры, решения зафиксированы 2026-09-16) + шаг 8 (Сегодня/спринты).
- **v0.23.0 (2026-09-15)** — шаги 0–5: ws · вложения (extraction) · история статусов · MD-карта · bulk · вердикты (только саммари) → `archive/PLAN.v0.23.md`, CHANGELOG [0.23.0]
- **v0.22.0 (2026-08-30)** — проекты/модули, Plane-срез, outbound, темы → `archive/PLAN.v0.22-projects-modules.md`, CHANGELOG
- **v0.21.0 / v20–v17** — helpdesk + AI → `archive/PLAN.v17-21.md`; **v0–16** (Plane-based) → `archive/PLAN.v0-plane-era.md`

## Методология
→ `CONTRIBUTING.md` раздел «Методология разработки» (режимы A/B/C, уровни «шаг»/«версия», чек-листы СТАРТА/ЗАКРЫТИЯ/ВЫПУСКА, стоп-гейт, нумерация, передача между сессиями).
